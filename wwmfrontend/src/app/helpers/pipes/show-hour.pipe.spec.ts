import { ShowHourStartPipe } from './show-hour-start.pipe';

describe('ShowHourPipe', () => {
  it('create an instance', () => {
    const pipe = new ShowHourStartPipe();
    expect(pipe).toBeTruthy();
  });
});
